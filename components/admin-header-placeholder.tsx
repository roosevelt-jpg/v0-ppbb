'use client'

// This is a placeholder component that does nothing
// The actual header is rendered by the admin layout
// Use this in place of AdminHeader to maintain backwards compatibility

export function AdminHeader({ title, subtitle }: { title: string; subtitle?: string }) {
  // Return nothing - header is handled by layout
  return null
}
